# Exact pins keep published metrics reproducible. Upgrade one dependency at a
# time, rerun `run_all --ml`, compare against MODEL_CARD, then commit. These
# versions target Python 3.13; development-only lint/coverage tools live in CI.
playwright==1.61.0
beautifulsoup4==4.15.0
pandas==2.3.3
numpy==2.5.1
matplotlib==3.11.1
requests==2.34.2
imagehash==4.3.2
Pillow==12.3.0
psycopg2-binary==2.9.12
SQLAlchemy==2.0.51
python-dotenv==1.2.2

# Price model
catboost==1.2.10
scikit-learn==1.9.0

# Survival analysis is part of the offline ML chain.
lifelines==0.30.3

# Test runner
pytest==9.1.1

# Web estimator and labelling interface
fastapi==0.141.1
uvicorn[standard]==0.52.1
