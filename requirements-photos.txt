# Тяжёлые зависимости для работы с изображениями — ОТДЕЛЬНО от requirements.txt.
#
# Почему отдельно: torch с torchvision весят около двух гигабайт. Ставить их
# всем, кто просто хочет собрать данные или обучить модель цены, — это долгая
# установка и такой же долгий CI ради кода, который в основном конвейере не
# выполняется ни разу.
#
# Что без них не работает:
#   kz/ml/photo_features.py   эмбеддинги ResNet50 и снижение размерности
#   kz/ml/photo_clip.py       zero-shot оценка состояния по фотографии
#
# Обе вещи — исследовательские, в run_all не входят. Замер показал, что
# признаки из фотографий цену не улучшают (см. README, «Что выяснилось про
# фотографии»), поэтому они и остались за пределами конвейера.
#
# Установка, когда понадобится:  pip install -r requirements-photos.txt

torch>=2.2
torchvision>=0.17
open_clip_torch>=2.24
