# Heavy image dependencies stay separate from the tabular environment because
# PyTorch and torchvision add roughly two gigabytes. These packages support
# experimental ResNet/CLIP work and are not part of the main pipeline.
# Install only when needed: `pip install -r requirements-photos.txt`.

torch==2.13.0
torchvision==0.28.0
open_clip_torch==3.3.0
